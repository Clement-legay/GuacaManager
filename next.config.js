/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost:3000',
            'guacatables.alternancerouen.fr'
        ],
    }
}

module.exports = nextConfig
